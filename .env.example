# Run: python3 scripts/configure.py
# First start stays on loopback. Create admin before enabling LAN access.
WEB_BIND=127.0.0.1
WEB_PORT=8080
TRACKER_BIND=127.0.0.1
OSMAND_PORT=5055
POSTGRES_PASSWORD=
